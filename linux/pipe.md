##linux pipe commands:
 * six categories: (鸟哥私房菜)
	 * select: `cut`, `grep`
	 * sort: `sort`, `uniq`, `wc`
	 * redirect: `tee`
	 * string: `tr`, `col`, `join`, `paste`, `expand`
	 * split: `split`
	 * argument: `xargs`